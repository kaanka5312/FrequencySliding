library(brms)
# Prior Predictive Check
priors <- c(
# Intercept (baseline PF)
prior(normal(0, 0.5), class = "Intercept"),
# Fixed effects for group, region_category, and their interaction
prior(normal(0, 0.25), class = "b"),
# Random intercept SDs (subjects, regions)
prior(exponential(1), class = "sd"),  # weakly informative
# Residual SD (sigma)
prior(exponential(1), class = "sigma")
)
fit_prior <- brm(
formula = value ~ group * region_type + (1 | subj_id) + (1 | region),
data = PF_long_list[[1]],
family = student(),
prior = priors,
sample_prior = "only",
chains = 4,
iter = 2000,
cores = 4
)
prior_preds <- posterior_predict(fit_prior)
ppc_dens_overlay(
y = PF_long_list[[1]]$value,
yrep = prior_preds[1:100, ]  # Use first 100 draws to avoid overplotting
)
library(bayesplot)
ppc_dens_overlay(
y = PF_long_list[[1]]$value,
yrep = prior_preds[1:100, ]  # Use first 100 draws to avoid overplotting
)
fit_list <- list()
fit_prior <- brm(
formula = value ~ group * region_category + (1 | subj_id) + (1 | region),
data = PF_collapsed_list[[1]],
family = student(),
prior = priors,
sample_prior = "only",
chains = 4,
iter = 2000,
cores = 4
)
prior_preds <- posterior_predict(fit_prior)
library(bayesplot)
ppc_dens_overlay(
y = PF_long_list[[1]]$value,
yrep = prior_preds[1:100, ]  # Use first 100 draws to avoid overplotting
)
fit_list <- list()
for (comp in names(PF_collapsed_list)) {
message("Fitting model for: ", comp)
fit_list[[comp]] <- brm(
value ~ group * region_category + (1 | subj_id) + (1 | region),
data = PF_collapsed_list[[comp]],
family = student(),
chains = 4,
iter = 2000,
cores = 6,
seed = 123  # Set seed for reproducibility
)
}
# Loop through each component's long data frame
for (comp in names(PF_collapsed_list)) {
message("Fitting model for: ", comp)
fit_list[[comp]] <- brm(
value ~ group * region_category + (1 | subj_id) + (1 | region),
data = PF_collapsed_list[[comp]],
family = student(),
prior = priors,
chains = 4,
iter = 2000,
cores = 6,
seed = 123  # Set seed for reproducibility
)
}
set.seed(123)
# Settings
n_subj <- 50
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.5, 0.3,
0.5, 1, 0.4,
0.3, 0.4, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 1, 1)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] +
random_effects[subj, 1] +
sum(beta[subscale, ] * pred) +
sum(random_effects[subj, 2:3] * pred)
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(ggpubr)
gghistogram(synthetic_data$BDI_score)
library(rstan)
# windows
setwd("C:/Users/kaank/OneDrive/Belgeler/GitHub/FrequencySliding/")
stan_model <- stan_model(model_code = "./external/MultLevel_BDI.stan")
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
summary(fit)
print(fit)
# Density plots (posterior distributions)
mcmc_areas(
posterior,
pars = c("intercept[1]", "intercept[2]", "intercept[3]"),
prob = 0.95
)
library(bayesplot)
# Extract posterior samples from rstan fit
posterior <- as.array(fit)  # 3D array: iterations × chains × parameters
mcmc_areas(
posterior,
pars = c("intercept[1]", "intercept[2]", "intercept[3]"),
prob = 0.95
)
dimnames(posterior)$parameters
posterior
dimnames(posterior)$parameters
mcmc_areas(
posterior,
pars = c("Rho[2,1]", "intercept[3,1]"),
prob = 0.95
)
mcmc_areas(
posterior,
pars = c("Rho[2,1]", "Rho[3,1]"),
prob = 0.95
)
L_random
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
setwd("C:/Users/kaank/OneDrive/Belgeler/GitHub/FrequencySliding/")
set.seed(123)
# Settings
n_subj <- 50
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 1, 1)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] +
random_effects[subj, 1] +
sum(beta[subscale, ] * pred) +
sum(random_effects[subj, 2:3] * pred)
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
# Settings
n_subj <- 150
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
predictors <- scale(predictors, center = TRUE, scale = FALSE)  # Centering
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 2, 2)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] +
random_effects[subj, 1] +
sum(beta[subscale, ] * pred) +
sum(random_effects[subj, 2:3] * pred)
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
# Settings
n_subj <- 150
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
predictors <- scale(predictors, center = TRUE, scale = FALSE)  # Centering
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 2, 2)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] +
random_effects[subj, 1] +
random_effects[subj, 2] * predictors[i, 1] +
random_effects[subj, 3] * predictors[i, 2] +
sum(beta[subscale, ] * predictors[i, ])
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
apply(random_effects, 2, sd)
# windows
setwd("C:/Users/kaank/OneDrive/Belgeler/GitHub/FrequencySliding/")
# Settings
n_subj <- 150
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
predictors <- scale(predictors, center = TRUE, scale = FALSE)  # Centering
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 2, 2)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] +
random_effects[subj, 1] +
random_effects[subj, 2] * predictors[i, 1] +
random_effects[subj, 3] * predictors[i, 2] +
sum(beta[subscale, ] * predictors[i, ])
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
n_subj <- 150
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
predictors <- scale(predictors, center = TRUE, scale = FALSE)  # Centering
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 2, 2)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] + random_effects[subj, 1] +
(beta[subscale, 1] + random_effects[subj, 2]) * predictors[i, 1] +
(beta[subscale, 2] + random_effects[subj, 3]) * predictors[i, 2]
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
n_subj <- 150
n_subscales <- 3
n_obs <- n_subj * n_subscales
n_predictors <- 2
# Simulate predictors
predictors <- matrix(rnorm(n_obs * n_predictors), ncol = n_predictors)
predictors <- scale(predictors, center = TRUE, scale = FALSE)  # Centering
# Simulate fixed intercepts & slopes (true values)
intercepts <- c(10, 15, 20)
beta <- matrix(c(2, -1, 1.5, 0.5, -2, 1), nrow = n_subscales, byrow = TRUE)
# Simulate random effects covariance structure
L_random <- chol(matrix(c(1, 0.7, 0.7,
0.7, 1, 0.7,
0.7, 0.7, 1), nrow = 3))  # Cholesky of random effects correlation
sigma_random <- c(2, 2, 2)  # SDs of random effects (intercept, slopes)
# Simulate random effects for subjects
z_random <- matrix(rnorm(n_subj * 3), nrow = n_subj, ncol = 3)
random_effects <- z_random %*% diag(sigma_random) %*% t(L_random)
# Subject & subscale indices
subj_id <- rep(1:n_subj, each = n_subscales)
subscale_id <- rep(1:n_subscales, times = n_subj)
# Generate outcome (BDI_score)
mu <- numeric(n_obs)
for (i in 1:n_obs) {
subj <- subj_id[i]
subscale <- subscale_id[i]
pred <- predictors[i, ]
mu[i] <- intercepts[subscale] + random_effects[subj, 1] +
(beta[subscale, 1] + random_effects[subj, 2]) * predictors[i, 1] +
(beta[subscale, 2] + random_effects[subj, 3]) * predictors[i, 2]
}
sigma <- 2  # residual SD
BDI_score <- mu + rnorm(n_obs, 0, sigma)
# Final data list for Stan
synthetic_data <- list(
n_obs = n_obs,
n_subj = n_subj,
n_subscales = n_subscales,
subj_id = subj_id,
subscale_id = subscale_id,
predictors = predictors,
BDI_score = BDI_score
)
str(synthetic_data)  # Check structure
library(rstan)
stan_model <- stan_model(file = "./external/MultLevel_BDI.stan")
# Fit model
fit <- sampling(
stan_model,
data = synthetic_data,  # your list of data
chains = 4,
iter = 2000,
seed = 123
)
print(fit, pars = c("Rho"), probs = c(0.025, 0.5, 0.975))
